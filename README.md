## PARTE A
1. C
2. B
3. B
4. B
5. B
6. B
7. B
8. B
9. C
10. C
11. C
12. B
13. C
14. B
15. B

## PARTE B
### Pregunta 1
El algoritmo es como una receta, es el conocimiento imperativo para resolver un problema, es una serie de pasos que se le da a seguir al ordenador para lograr algo.
Para resolver un problema de programación, lo primero que hay que hacer es entenderlo, una vez que se entiende el problema hay que pensar en que tipo de algoritmo es más conveniente,
y finalmente hacer el código del programa, probar si funciona y si no volverlo a intentar.

### Pregunta 2
La diferencia principal es que print solo muestra el valor en pantalla y return sirve primero para dar por terminado la función y segundo cuando se usa la función devuelve el valor.
Esto para que pueda ser utilizado después en otro bloque de código.

### Pregunta 3
Yo interpreto el scope de una variable como el lugar donde la variable vive, por ejemplo si una variable está dentro de una función solo puede vivir allí.
Pero si está afuera, afecta y vive en el resto del programa.Las variables locales son aquellas que solo pueden vivir en un espacio determinado porque sino se "mueren".
como las variables que se definen dentro de una función o un bucle. Las variables globales pueden vivir en todo el código.

### Pregunta 4
Una lista en Python es un conjunto mutable de elementos que están ordenados. 
Lo haría así:
lista = []
a = 0
for x in range (10):
  a += 1
  lista.append(a)

### Pregunta 5
El método init es super útil en una clase porque lo que hace es inicializar una variable dentro de la clase. self sirve para que la variable dentro de la clase pueda 
referirse a si misma, guardar valores en ella misma o llamar sus propias variables y funciones.
  
  
  
